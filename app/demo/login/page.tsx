import { Section } from "@/components/site/Section";

export default function DemoLoginPage() {
  return (
    <Section eyebrow="Demo" title="Demo access">
      <p>This placeholder page is where a lightweight shared-password gate should go. Use DEMO_PASSWORD and middleware or a server action in the next implementation pass.</p>
    </Section>
  );
}
